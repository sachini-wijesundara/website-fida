/**
 * src/components/admin/index.ts
 *
 * Admin-only components such as data tables, stat cards, and action menus.
 * Re-export from this barrel file to keep imports clean:
 *   import { StatCard } from "@/components/admin";
 */

// Exports added here as admin components are created.
export {};

/**
 * src/components/forms/index.ts
 *
 * Reusable form field components — inputs, selects, file pickers, and submit buttons.
 * Re-export from this barrel file to keep imports clean:
 *   import { FormInput } from "@/components/forms";
 */

// Exports added here as form components are created.
export {};
